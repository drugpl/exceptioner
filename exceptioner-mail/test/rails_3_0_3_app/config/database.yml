<% adapter = "sqlite3" %>
# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: <%= adapter %>
  host: localhost
  database: db/exceptioner_test_app_development

test:
  adapter: sqlite3
  host: localhost
  database: db/exceptioner_test_app_development

